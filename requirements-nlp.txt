# =============================================================================
# NLP Agent 重型模型依赖（约 1GB，含 PyTorch + transformers）
# 安装：pip install -r requirements-nlp.txt
#
# 适用场景：当且仅当你需要"重新训练模型"或"离线灌库"时才装。
# 具体到本项目，下列脚本会触发模型加载：
#   - python -m agents.nlp_agent.tools.sentiment   --backfill   # 葡语情感离线灌库
#   - python -m agents.nlp_agent.tools.topic_model --backfill   # BERTopic 主题训练
#
# 不适用场景（无需安装本文件）：
#   - 业务在线查询：所有 aggregate_* 函数仅读 MySQL 表，毫秒级
#   - decision_agent 生成报告：消费 nlp_agent 写入的 state["review_insights"]
#   - 关键词主题分类 / 词云数据：纯字符串处理 + SQL，已在 requirements.txt 覆盖
#
# 数据库中已经存在落库结果（review_sentiment / review_topics / review_topic_meta）的话，
# 完全不需要本文件中的依赖。
# =============================================================================

# 葡语情感模型（pysentimiento/bertweet-pt-sentiment）
pysentimiento

# BERTopic 主题建模（含 sentence-transformers + UMAP + HDBSCAN）
bertopic
sentence-transformers
hdbscan

# transformers 与 torch 会被上述包自动拉取；如需指定版本请在此处 pin
# torch>=2.0
# transformers>=4.30