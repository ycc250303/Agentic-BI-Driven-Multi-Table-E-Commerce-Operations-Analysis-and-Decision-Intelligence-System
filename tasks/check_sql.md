我现在需要你完成一项任务，任务步骤和要求包括：

1.根据 `create_origin_table.sql`、`create_materialized_views.sql`和`schema_dictionary.md`对数据库的说明，生成以下问题的sql语句：

```
TEST_QUESTIONS = [
    "2017 年 GMV 是多少？按月和各州排名的趋势怎样？",
    "平台整体准时交付率是多少？哪些州延迟最严重？",
    "哪种支付方式最受欢迎？平均分期数是多少？",
    "产品的重量、尺寸与运费之间有什么关系？",
    "Top 10 差评品类是什么？",
    "2017年哪个州的销售额最高？交付准时率是多少？哪种支付方式最受欢迎？",
    "哪些卖家的差评率最高？",
]
```

2.生成完成后，启动虚拟环境`conda activate bi`，运行 `agents/sql_agent/run.py`，记录程序生成的sql语句

你需要完成的任务包括：

1.分析二者生成的sql语句的差异，并判断哪边是正确的（或者都不正确），如果 `run.py` 生成的sql语句不正确，分析原因并解决，比如优化提示词，增加工具流程等
2.判断`run.py`能否一次生成多个问题的sql（比如，用户一次性提问“哪种支付方式最受欢迎？平均分期数是多少？”，`run.py` 生成的sql是否能同时解决这两个问题）

约束：
1.禁止修改check_sql.md文件
2.提前用一个sql文件，存储你针对提问生成的sql语句